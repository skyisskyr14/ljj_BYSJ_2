server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cloud_yearbook?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: rRr1352-
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 3000
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 10MB

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto

app:
  jwt:
    secret: cloud-yearbook-secret-key-cloud-yearbook-2026
    expire-hours: 24
  upload-dir: uploads
